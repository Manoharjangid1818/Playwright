{
  "name": "qa360",
  "version": "1.0.0",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm --prefix dashboard run build",
    "test": "npm --prefix dashboard run test -- --watchAll=false"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "react-scripts": "^5.0.1"
  }
}
